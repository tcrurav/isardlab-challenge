rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Session collection logic
    // We allow read and write freely for the game logic prototype.
    // In production, you would restrict this to authenticated users using Firebase Anon Auth.
    match /sessions/{sessionId} {
      allow read, write: if true;
    }
  }
}
